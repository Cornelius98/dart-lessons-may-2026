void main() {
  
  //Final - Values that update at run time, example DateTime.now()
  final currentTime = DateTime.now();
  print(currentTime);

  //Const - saves values that are known before run-time
  const pi = 3.146;
  print(pi);

  pi = 20;
  print(pi);

}